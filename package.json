{
  "private": true,
  "name": "biozar",
  "version": "1.1.0",
  "description": "BIOZAR – Console de Pilotage (100% gratuit, Cloudflare + Supabase)",
  "scripts": {
    "dev": "npx serve .",
    "android": "npm --prefix biozar-app run android",
    "lint": "npm --prefix functions run lint && npm --prefix ai run lint"
  },
  "browserslist": [
    "last 2 versions",
    "not dead"
  ],
  "dependencies": {
    "@google-cloud/appengine-admin": "^4.2.2",
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2"
  }
}
